<style>
/* Login page — exact layout values from maquette */
.login-page {
	display: flex;
	flex-direction: column;
	align-items: flex-start;
	padding: 0px;
	width: 100%;
	min-height: 100vh;
	background: #F9FAFB;
}

.login-container {
	display: flex;
	flex-direction: column;
	justify-content: center;
	align-items: center;
	padding: 284px 32px;
	gap: 32px;
	width: 100%;
}

/* Top block (icon + title + subtitle) */
.login-top {
	width: 448px;
	display: flex;
	flex-direction: column;
	align-items: center;
	justify-content: center;
}

.login-icon { width:48px; height:48px; display:flex; align-items:center; justify-content:center; margin-bottom:12px; }
.login-icon-img, .login-icon-fallback { width:48px; height:48px; border:4px solid #155DFC; border-radius:6px; background:#DBEAFE; display:block; }

.login-title {
	font-family: 'Arimo', sans-serif;
	font-weight: 700;
	font-size: 30px;
	line-height: 36px;
	text-align: center;
	color: #101828;
	margin-top: 6px;
}

.login-subtitle {
	font-family: 'Arimo', sans-serif;
	font-weight: 400;
	font-size: 14px;
	line-height: 20px;
	text-align: center;
	color: #4A5565;
	margin-top: 6px;
}

/* Card */
.login-card {
	display: flex;
	flex-direction: column;
	align-items: flex-start;
	padding: 32px 40px 48px;
	width: 448px;
	background: #FFFFFF;
	box-shadow: 0px 8px 24px rgba(16,24,40,0.06);
	border-radius: 10px;
}

.login-form {
	display:flex;
	flex-direction: column;
	align-items: flex-start;
	padding: 0px;
	gap: 24px;
	width: 368px;
	margin: 0 auto;
}

.form-group { display:flex; flex-direction:column; gap:4px; width:100%; }
.form-label { font-family: 'Arimo', sans-serif; font-size:14px; color:#364153; }
.form-input { box-sizing: border-box; display:flex; align-items:center; padding:8px 12px; height:42px; background: rgba(255,255,255,1e-05); border:1px solid #D1D5DC; box-shadow: 0px 1px 3px rgba(0,0,0,0.1), 0px 1px 2px -1px rgba(0,0,0,0.1); border-radius:8px; }
.form-input input { width:100%; border:0; outline:none; font-size:16px; color:#0A0A0A; background:transparent; }
.btn-primary {
	box-sizing: border-box;
	display:block;
	padding:8px 16px;
	width:368px;
	height:38px;
	background:#155DFC;
	box-shadow: 0px 1px 3px rgba(0,0,0,0.08), 0px 1px 2px -1px rgba(0,0,0,0.04);
	border-radius:8px;
	color:#FFFFFF;
	border:0;
	cursor:pointer;
	margin: 12px auto 0 auto;
}
.create-account-link { color:#155DFC; text-decoration:none; }

@media (max-width: 900px) {
	.login-container { padding: 48px 16px; }
	.login-container { padding-left: 32px; }
}

</style>
