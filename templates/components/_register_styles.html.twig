<style>
/* Page register layout */
.register-page {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  padding: 0;
  width: 100%;
  min-height: 100vh;
  background: #F9FAFB;
}

.register-container {
  display: flex;
  justify-content: center;
  width: 100%;
  padding: 149px 32px 149px 32px;
}

.register-card {
  width: 448px;
  background: #FFFFFF;
  box-shadow: 0px 1px 3px rgba(0,0,0,0.1), 0px 1px 2px -1px rgba(0,0,0,0.1);
  border-radius: 10px;
  padding: 32px 40px 24px;
}

.register-header {
  text-align: center;
  margin-bottom: 8px;
}
.register-title {
  font-family: Arimo, sans-serif;
  font-weight: 700;
  font-size: 30px;
  line-height: 36px;
  color: #101828;
}
.register-subtitle {
  font-family: Arimo, sans-serif;
  font-weight: 400;
  font-size: 14px;
  color: #4A5565;
}

.register-form { width: 368px; margin: 0 auto; }
.form-row { display: flex; gap: 16px; }
.form-group { display: flex; flex-direction: column; gap: 4px; width: 100%; }
.form-label { font-family: Arimo, sans-serif; font-size: 14px; color: #364153; }
.form-input { box-sizing: border-box; display:flex; align-items:center; padding:8px 12px; height:42px; background: rgba(255,255,255,0.00001); border:1px solid #D1D5DC; border-radius:8px; }
.form-input input { width:100%; border:0; outline:none; font-size:16px; color:#0A0A0A; background:transparent; }

.btn-primary { display:inline-flex; align-items:center; justify-content:center; padding:8px 16px; background:#155DFC; color:#fff; border-radius:8px; border:0; cursor:pointer; }

.login-link { color:#155DFC; text-decoration:none; }

@media (max-width: 640px) {
  .register-container { padding: 48px 16px; }
  .form-row { flex-direction: column; }
}
</style>
